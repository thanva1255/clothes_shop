<div class="show-page-container">
    <div class="h-dvh">
        <img src=<%= @product.image_url %> alt="">
    </div>

    <div class="product-detail">
        <div class="product-basic-detail">
            <p class="mt-5 font-light"><%= @product.collection.name %></p>
            <p class="mt-2 text-2xl font-bold"><%= @product.name %></p>

            <div class="mt-5">
                <p class="font-bold">฿ 100</p>
            </div>
        </div>

        <p class="mt-3">size</p>

        <div class="mt-4 flex gap-3">
            <button class="button-size">S</button>
            <button class="button-size">M</button>
            <button class="button-size">L</button>
            <button class="button-size">XL</button>
        </div>

        <div>
            <p class="mt-10"><p><%= @product.description %></p>
        </div>
    </div>
</div>